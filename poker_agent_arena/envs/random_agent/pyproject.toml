[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "random-agent-env"
version = "0.1.0"
description = "Isolated environment for RandomAgent"
requires-python = ">=3.8"
dependencies = [
    "PyPokerEngine",
    "pyyaml>=6.0",
]
